import { OrderStatusIconDirective } from './order-status-icon.directive';

describe('OrderStatusIconDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderStatusIconDirective();
    expect(directive).toBeTruthy();
  });
});
