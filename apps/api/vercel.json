{
  "version": 2,
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.ts"
    }
  ],
  "buildCommand": "prisma generate && prisma migrate deploy && npx nx build api",
  "outputDirectory": "dist/apps/api"
}