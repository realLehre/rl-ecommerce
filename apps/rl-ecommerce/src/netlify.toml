[build]
  command = "npx nx build rl-ecommerce --configuration=production"
  publish = "dist/apps/rl-ecommerce/browser"
  base = "/"

[build.environment]
NODE_VERSION = "18"
NETLIFY_USE_YARN = "false"
NX_WORKSPACE = "true"

  [[redirects]]
    from = "/*"
    to = "/index.html"
    status = 200
    force = true

