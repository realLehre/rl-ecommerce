import { ItemsShowingPipe } from './items-showing.pipe';

describe('ItemsShowingPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemsShowingPipe();
    expect(pipe).toBeTruthy();
  });
});
