import { NumberOfFiltersPipe } from './number-of-filters.pipe';

describe('NumberOfFiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberOfFiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
