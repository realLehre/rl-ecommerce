import { DeliveryEventRemarkPipe } from './delivery-event-remark.pipe';

describe('DeliveryEventRemarkPipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryEventRemarkPipe();
    expect(pipe).toBeTruthy();
  });
});
